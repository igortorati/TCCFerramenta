package ui;

import graph.Matrix;

public class SaveData {
	Matrix matrix;
	Parameters parameters;
	
	public SaveData(Matrix matrix, Parameters parameters) {
		this.matrix = matrix;
		this.parameters = parameters;
	}
}